# Development requirements for ELRO Connects Real-time integration

# Code formatting and linting.
# Keep these pins in sync with .github/workflows/ci.yml, or a local run and CI
# will disagree about formatting.
black==24.2.0
isort==5.13.2
flake8==7.0.0
mypy==1.9.0

# Testing
pytest==8.4.1
pytest-asyncio==1.1.0
pytest-cov==6.2.1
pytest-homeassistant-custom-component==0.13.269

# K2 protocol library (also declared in manifest.json; needs Python >= 3.12)
elro-connects-k2-protocol==0.1.0

# Home Assistant core (for development and testing)
homeassistant==2025.8.0

# Type stubs
types-requests==2.31.0.20240125
